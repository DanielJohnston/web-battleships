<meta http-equiv="refresh" content="4" />
WAITING ON OPPONENT TO FIRE.
<h3>Your board</h3>
<br><pre><%= $game.own_board_view $game.send(session[:player_id]) %></pre>
<br>
<h3>Opponent's board</h3>
<br><pre><%= $game.opponent_board_view $game.send(session[:player_id]) %></pre>
<br>
<a href="/game/play">Check if we're ready to play.</a>
